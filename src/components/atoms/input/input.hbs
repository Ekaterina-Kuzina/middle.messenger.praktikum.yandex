<div class="input{{#if className}} {{className}}{{/if}}">
    <label for="{{ name }}" class="{{#unless lableLeft}}input__title{{/unless}}">{{ title }}</label>
    <input name="{{ name }}" value="{{ value }}" {{isReadonly readonly}} class="{{#unless lableLeft}}input__field{{/unless}}">
</div>
